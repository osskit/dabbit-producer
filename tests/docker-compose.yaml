version: '3.8'

services:
  producer:
    build: ../
    ports:
      - 3000:3000
    environment:
      - PORT=3000
    depends_on:
        - rabbitmq
  rabbitmq:
    image: rabbitmq:3-management-alpine
    container_name: 'rabbitmq'
    ports:
        - 5672:5672
        - 15672:15672
version: '3.8'

services:
  producer:
    build: ../
    ports:
      - 3000:3000
    environment:
      - PORT=3000
  mocks:
    image: wiremock/wiremock
    ports:
        - 8080:8080
    command: '--verbose'